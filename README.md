# webscraping-cheerio
### Installation
Clone the repository.


Run the following command in the folder
```sh
npm install
```

Start scraping
```sh
node index.js
```

The output will be stored in movies.csv .
